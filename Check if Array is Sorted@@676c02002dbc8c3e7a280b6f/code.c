#include <stdio.h>
int main(){
    int N;
    scanf("%d",&N);
    int arr[N];
    for(int i = 0;i<N;i++){
        scanf("%d",&arr[i]);
    }
    int i;
    int ar[i];
        
        if(ar[i]<=arr[N-1] ){
            printf("Sorted");
        }
        else{
            printf("Not Sorted");
        }
}